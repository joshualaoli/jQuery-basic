/**

Manipulating text 
$ (`p`).addClass (`paragraph margin50`);

$ (`h1`).text(`Hey bro!`);

$ (`button`).html (`<em>click here!</em>`);

$(`img`).attr (`src`, `./Assets/images/crash.png`);

$(`a`). attr (`href`, `https://www.yahoo.com/`);

----------adding event listener with jQuery---------

$ (`button`).click (function() {
    $(`h1`).css (`color`, `red`);
})

$(`input`).keypress(function(event) {
    console.log(event.key)
})
*/

$